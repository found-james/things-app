const React = require('react');
const Default = require('./Default.jsx');

class Show extends React.Component {
    render() {
        return (
        <Default>
            <section>
                testing
            </section>
        </Default>
        )
    }
}

module.exports = Show;